<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= election.electionName%> | Preview</title>
    <link rel="stylesheet" href="./css/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-500 font-mono">
    <div class="grid grid-cols-6">
      <div class="col-start-3 col-span-3 py-10">
        <div class="flex-auto">
          <h1 class="text-3xl font-semibold text-gray-600">
            Online Voting Platform
          </h1>
          <div class="mt-5">
            <a href="/elections/<%= election.id%>"
              ><h2 class="text-2xl font-extrabold">
                <%= election.electionName %>
              </h2></a
            >
          </div>
          <form>
            <ul class="list-none">
              <% for(let i = 0; i < questions.length; i++) { %>
              <li class="question-Item">
                <div
                  class="w-fit my-2 px-2 py-1 items-center rounded hover:bg-purple-50"
                >
                  <div class="mt-5">
                    <h3 class="text-xl font-bold">
                      <%= i + 1%>. <%=questions[i].title%>
                    </h3>
                    <span class="text-sm font-normal"
                      ><%=questions[i].description%> ?</span
                    >
                  </div>
                  <ul class="list-none">
                    <% for(let j = 0; j < options[`${i}`].length; j++) { %>
                    <li class="question-Item">
                      <div
                        class="flex w-fit px-2 items-center rounded hover:bg-purple-50"
                      >
                        <div class="mt-2">
                          <input
                            type="radio"
                            name=""
                            id=""
                            value="<%=options[`${i}`][j].title%>"
                          />
                          <span class="text-sm font-medium"
                            ><%=options[`${i}`][j].title%></span
                          >
                        </div>
                      </div>
                    </li>
                    <% } %>
                  </ul>
                </div>
              </li>
              <% } %>
            </ul>
            <button
              title="Button is disabled on preview"
              type="submit"
              class="ml-20 mt-4 text-white bg-green-500 hover:bg-green-200 font-medium rounded text-sm px-5 py-2 mr-2 mb-2"
            >
              Vote
            </button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
